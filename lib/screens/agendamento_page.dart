import 'package:flutter/material.dart';

class AgendamentoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Agendamento')),
      body: Center(child: Text('Página de agendamento')),
    );
  }
}

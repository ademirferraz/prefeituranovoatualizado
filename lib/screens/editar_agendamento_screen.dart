import 'package:flutter/material.dart';

class EditarAgendamentoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Editar Agendamento')),
      body: Center(child: Text('Tela de edição de agendamento')),
    );
  }
}
